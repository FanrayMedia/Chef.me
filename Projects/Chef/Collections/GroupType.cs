namespace Chef.Collections
{
    /// <summary>
    /// The different types of items a user might possibly collect on our site
    /// such as other user, recipes etc.
    /// </summary>
    public enum GroupType : byte 
    {
        User = 0,
    }
}
